# Database Configuration
# Copy this file to .env and update with your actual credentials
# For production, use environment variables instead of this file

DB_HOST=localhost
DB_NAME=portfolio_db
DB_USER=root
DB_PASS=

# For production deployment, set these as server environment variables instead
# Example for Apache: SetEnv DB_HOST "localhost"
# Example for Nginx with PHP-FPM: Add to php-fpm pool configuration
